<h2>เปลี่ยนรหัสผ่าน</h2>
<% if (error) { %><div><%= error %></div><% } %>
<% if (success) { %><div><%= success %></div><% } %>
    <form method="POST" action="/profile/changepassword">
        <input type="password" name="oldPassword" placeholder="รหัสเดิม">
            <input type="password" name="newPassword" placeholder="รหัสใหม่">
                <button type="submit">เปลี่ยน</button>
            </form>
